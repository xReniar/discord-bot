# discord-bot

## dependencies
- discord.js
- express.js

## configurations
The `conf.json` has this structure:
```json
{
    "discord": {
        "token":"bot-token",
        "guild_id":"guild-id",
        "client_id":"bot-id"
    },
    "server": {
        "port": 8080
    }
}
```